#include <iostream>
#include <conio.h>
using namespace std;

 main()
{
    void vote (int);
    int age;
    cout<<"Enter your age \n";
    cin>>age;
    vote(age);
    

    return(0);
}
void vote (int x)
{
    if(x>=18)
    cout<<"You are eligible to vote";
    else
    cout<<"You are not eleigible to vote";
}
